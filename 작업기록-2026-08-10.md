# 2026-08-10 작업 기록 — 수정사항 & 트러블슈팅 전체

시각장애인 안내 로봇(Stretch SE3) 세션. 대시보드 ROS 먹통, 주행 멈춤,
블랙박스 로깅, 엘베 운영, RViz 표시 문제를 규명·수정한 기록.

---

## ⭐ 1. 대시보드 ROS 먹통 = 배터리 NaN 크래시 (오늘 최중요)

### 증상
- 대시보드(main.py) 실행 몇 초~수십 초 뒤 **ROS 절반이 통째로 먹통**:
  `/robot_pose` 미수신, `/battery` null, 노드 4개만 보임, **지도전환(load_map) timeout**.
- Flask(8080 웹)는 멀쩡한데 ROS 기능만 죽음. **매 세션 반복**.

### 오진 → 정정
- 처음엔 **FastDDS 섬 현상**(탐색은 되나 데이터 채널 잠김)으로 의심.
- 로그를 깊이 파보니 **섬이 아니라 그냥 파이썬 버그**였음. (사용자가 "로그 보면 되잖아" → 정답)

### 진짜 원인
1. Stretch 드라이버가 `/battery`의 `percentage`를 **NaN**으로 발행 (voltage 13.4V만 유효).
2. `_battery_callback`: `round(msg.percentage*100) if p<=1.0 else round(p)` →
   `NaN<=1.0`은 False → `round(NaN)` → **ValueError: cannot convert float NaN to integer**.
3. 콜백에 **try/except 없음** + 스핀에 **복구 없음** → 첫 배터리 메시지에서 `rclpy.spin` 스레드 **영구 사망**.
4. 그 뒤 pose·battery·서비스응답(load_map) 전부 얼어붙음 → "섬"처럼 보였던 것.

### 근거
- `/battery` 프로브: `percentage=nan (NaN? True) voltage=13.34 status=0`.
- `round(float('nan'))` 재현 → ValueError.
- system 블랙박스 로그가 부팅 3초 뒤 HB 멈춤(스핀 사망 시점) 실측.

### 수정 (main.py)
- `_battery_callback`: NaN/inf 방어 → `pct=None`(전압만 사용), 전체 try/except.
- `_amcl_pose_cb`, `_cmdvel_callback`: try/except (콜백이 스핀 못 죽이게).
- ROS 스핀을 **복구 루프**로: `while rclpy.ok(): try: spin except: log+sleep` (죽어도 재개).

### 결과
- 재시작 후 pose 실제값, battery voltage 정상, HB 계속, **지도전환(4층) 성공**.

---

## 2. nav 블랙박스 → 독립 프로세스 (robot_diag_nav.py 신규)

### 문제
- 기존 `robot_diag`는 대시보드 노드에 `attach`(얹힘) → 대시보드 스핀이 죽으면(위 배터리 버그)
  블랙박스도 **같이 침묵**. 실측: system 로거가 16:27:55 이후 **49분 침묵** → 리턴 주행 멈춤을 못 잡음.

### 해결
- `/rosout`(nav2 로그) 캡처를 **독립 프로세스**로 분리 → 자체 participant + 자체 스핀 + 복구 루프.
- 대시보드가 죽어도/섬에 갇혀도 **계속 기록**. (검증: 대시보드 diag 죽은 시각에도 nav 블랙박스는 정상 캡처)
- 대시보드가 `start_subprocesses`에서 **자동 spawn**(interface·vision처럼) → 수동 실행 불필요.
- 기록: `~/.ros/robot_diag/nav/`, 7일 보관. 8080에 **"주행" 탭** 추가.

### 파일
- `robot_diag_nav.py`(신규), `main.py`(자동 spawn + NAV2 탭 + 소스 등록),
  `robot_diag.py`(중복된 in-dashboard rosout 캡처 삭제 — 이제 독립 프로세스가 담당).
- ※ `robot_diag.py`의 `_NAV_NODES/_NAV_KW/_LVL_NAME`은 robot_diag_nav가 import하므로 **유지**.

---

## 3. 주행 멈춤(5초/30초) = nav + 엘베 동시 과부하

### 증상
- 승차지점 주행 중 5초마다 멈추고 30초씩 정지. "어느 순간부터" 재현되는 회귀.

### 원인
- **nav + 엘베앱(OCR 서버·그리퍼 카메라) 동시 구동 → CPU 과부하** →
  센서(라이다·뎁스)가 **~0.4초 지연** → costmap이 TF에 못 맞춰 **838회 드롭**
  ("timestamp earlier than transform cache") → 로봇 눈멀고+길잃음 → controller patience 초과 → 멈춤.

### 근거
- 엘베 **켠** 주행: 838드롭 + 19 patience + 멈춤, 도착 실패.
- 엘베 **끈** 주행: 0드롭 + 정상 도착. → 부하 확정.

### 해결 (운영 방식으로)
- **nav과 엘베를 동시에 돌리지 않는다.** 대시보드에 **"🛗 엘리베이터 앱 실행" 토글** 추가:
  - **켜기**: 엘베앱 실행 → 5000 뜨면 **제어권 자동 부여** + **armleft 자동 종료**(팔 넘김).
  - **끄기**: **제어권 회수(즉시 정지)** + 앱 SIGTERM(서보·바퀴 정리) → **주행 복귀**.
  - 엘베앱은 시작 시 8080 감지되면 authority=False로 대기 → 대시보드가 5000 뜬 뒤 부여(핸드셰이크).
- **제어권 토글을 앱 실행 토글로 통일**(중복 제거). 단축키 5.
- 운영 순서: 승차지점 주행(엘베 OFF) → 도착 후 엘베 토글 ON → 버튼 → OFF → 주행 복귀.

### 파일: main.py

---

## 4. 엘베 시나리오 버튼 = 정지 토글

### 요청
- 여정 6단계(① 호출 press · ② 문앞 정렬 · ③ 문 열림 대기 · ④ 탑승 · ⑤ 층 press · ⑥ 하차)
  중 자동 안무가 도는 단계(②④⑥)에서 **같은 버튼을 다시 누르면 그 동작이 멈추게**.

### 구현 (elevator_button_press/main.py `scene_set`)
- 같은 씬(prev==n) + **자동 안무 진행 중(`_auto_busy`)** 재클릭 →
  `_step_abort=True` + 재실행 생략 = **정지**. (다시 누르면 잔여부터 재개 — 기존 이어하기 유지)
- Esc(`/step_stop`)는 그대로. 버튼 정지는 **추가** 기능.

---

## 5. RViz 맵/costmap 안 뜸 = QoS 불일치 (조사만, 파일은 원복)

### 증상
- 층 전환(load_map) 후 RViz에서 **맵 사라짐**, **장벽(costmap 팽창) 얇게** 보임.

### 원인
- `map_server`·`global/local_costmap` 전부 **Transient Local(latched)** 발행 —
  `ros2 topic info -v`로 확인.
- 근데 `robot_view.rviz`의 Map·costmap 디스플레이가 **Volatile** 구독 →
  latched 재발행(층 전환 시)을 **놓쳐서** 사라지고 팽창 오버레이가 안 뜸.
- TF는 정상(map→odom, map→base_link 다 있음) → TF 문제 아님.

### 조치
- 임시로 TL로 바꿔 확인했으나 **사용자 요청으로 원복(Volatile)**.
- 향후 해결법: RViz Map/costmap 디스플레이 각각 Topic → Durability Policy = **Transient Local**.
- ⚠️ `robot_view.rviz`는 **repo 밖(`~/robot_view.rviz`)** 이라 버전관리 미적용.
  참조: `main.py`(RViz 실행 경로), `navigation-and-slam-guide.md`. → 추후 repo로 옮길지 결정.

---

## 6. 지도(층) 전환 & 하차지점 초기화 (동작 정리)

- **지도 전환**: `map_server` load_map 서비스로 **재시작 없이** 전환(floor4.yaml 성공, `_current_floor` 갱신).
  지도 3개(all=5층, floor4, floor3) 실존, 동일 좌표 복붙.
- **하차지점 초기화(init_exit)**: 전환 직후 AMCL 초기 위치를 `location.yaml`의 `엘리베이터 하차지점`
  좌표(x:-48.232, y:4.9318, z:0.610832, w:0.79176)로 자동 설정 = **RViz "2D Pose Estimate" 자동화**.
  엘베로 층 이동 시 로봇 위치를 새 층 하차지점으로 재정위. 동일 좌표라 어느 층이든 재사용.
- 테스트(실제 엘베 없이): A) 층 전환만 → B) 하차지점 초기화로 AMCL 점프 확인 → C) 실제 하차지점에 놓고 전환+주행.

---

## 7. 기타 규명 / 확인

- **stretch_driver 2개 = 중복 아님** (307020은 306651의 자식 프로세스, 한 쌍). 정상.
- **vision_assistant.py 47.7% CPU** — idle에도 높음. 추후 최적화 여지(제일 수상).
- **로깅 부담 없음**: nav 블랙박스 1.4% CPU, 초당 ~15줄. 최적화 불필요.
- 전체 부하 load 8.4 / 16코어 ≈ 52% — 여유 있음. 다만 nav+엘베 동시는 여전히 금물.
- CPU 하마: stretch_driver(93%+72%), rviz2(89%, 테스트용), realsense(70%+12%), controller(58%).

---

## 변경 파일 요약
| 파일 | 내용 |
|---|---|
| `main.py` (대시보드) | 배터리 NaN 수정·콜백 보호·스핀 복구 / nav 블랙박스 자동 spawn·NAV2 탭 / 엘베 앱 토글 통일 / attach rosout 제거 |
| `robot_diag_nav.py` (신규) | 독립 프로세스 nav 블랙박스 |
| `robot_diag.py` | 중복 rosout 캡처 제거(독립 프로세스로 이관) |
| `elevator_button_press/main.py` | 시나리오 버튼 정지 토글 |
| `~/robot_view.rviz` | (조사 후 원복 — repo 밖이라 커밋 대상 아님) |
