# Virtual environments
venv_tf115/
venv/
.venv/
people_tracker/people_tracker/

# OCR-RCNN external repo & model files
ocr-rcnn-v2/
*.zip
frozen_model/
frozen_model.pb
*.pb

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# ROS2 build artifacts
build/
install/
log/

# Test result images
results/

# venv
venv_ocr/

# 스냅샷(VLM 매핑용 원본 프레임) — 사용자 결정(2026-08-31)으로 저장소 내
# src/blind_nav_system/blind_nav_system/snapshots/ 에 저장하고 git으로 추적함.
# (깊이 PNG 포함이라 용량 커질 수 있음 — 많아지면 git-lfs 검토)

# 모자이크 전 원본 — 타인 얼굴이 그대로 들어 있다. 공개 저장소에 올리지 말 것
# 공개용은 docs/images/coex-demo.gif 뿐이고 그건 모자이크된 것이다
files/인재양성대전/코엑스_부스_원본_*.png
files/인재양성대전/코엑스_부스_촬영_원본.mov
IMG_*.mov
스크린샷*.png
